#version 150

// For object
in  vec3 in_Position;
in  vec3 in_Normal;
in  vec2 in_TextCoord;

uniform mat4 totalMatrix;

void main(void)
{
	gl_Position = vec4(in_Position, 1.0);//totalMatrix*vec4(in_Position, 1.0);
}

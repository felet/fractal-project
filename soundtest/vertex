#version 150

in  vec3 in_Position;
in  vec3 in_Normal;
in  vec2 in_TextCoord;

//uniform mat4 totalMatrix;

out vec2 tst;

void main(void)
{
    tst = in_TextCoord;
	gl_Position = vec4(in_Position, 1.0);
}
